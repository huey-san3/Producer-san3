# Producer-SAN3 Environment Config
# Copy this to .env and fill in values if needed
# Currently no API keys required — fully offline tool

# Optional: override default output directory
# OUTPUT_DIR=outputs
